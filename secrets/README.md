# Secrets

Do not commit plaintext credentials or key material here. Secret management is
planned but not implemented. The active configuration expects the private file
`/saved/secrets/nix.conf` outside this repository.
